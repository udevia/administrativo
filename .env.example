# Copiar a .env y ajustar:  cp .env.example .env
# (docker compose / Portainer interpolan estas variables)

# --- Base de datos ---
DB_PASSWORD=mi_erp_seguro
DB_ROOT_PASSWORD=root_seguro

# --- Usuario administrador inicial (solo se usa en el primer arranque) ---
ADMIN_USER=admin
ADMIN_PASSWORD=admin123
ADMIN_NAME=Administrador General
